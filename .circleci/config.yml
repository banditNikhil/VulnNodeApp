version: 2.1

jobs:
  # building the example app in docker
  build:
    docker:
      - image: circleci/buildpack-deps:stretch
    steps:
      - checkout
      - setup_remote_docker

  # SAST fortify
  SAST:        
      - run:
          name: SAST test on ssh with Fortify
          command: |
            ssh ubuntu@ec2-3-236-145-132.compute-1.amazonaws.com "./scan_node_app.sh"

workflows:
  version: 2
  # dependency check and deploy
  SASTtest:
    jobs:
      - SAST
