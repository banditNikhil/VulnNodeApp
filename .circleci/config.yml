version: 2.1
jobs:
  # deploy docker to server
  SAST:
    docker:
      - image: circleci/buildpack-deps:stretch
    steps:
      - checkout
      - setup_remote_docker
      - run: ssh-keyscan ec2-3-236-145-132.compute-1.amazonaws.com >> ~/.ssh/known_hosts
#       - add_ssh_keys:
#           fingerprints:
#             - "a2:a0:95:0a:38:c3:cb:6d:52:53:0c:92:99:05:d5:f3"
      - run:
          name: SAST test on ssh with Fortify
          command: |
            ssh ubuntu@ec2-3-236-145-132.compute-1.amazonaws.com "./scan_node_app.sh"

workflows:
  version: 2
  # dependency track before deploy
  sast-fortify:
    jobs:
      - SAST
