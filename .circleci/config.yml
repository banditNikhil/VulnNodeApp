version: 2.1
jobs:
  # build app in docker
  build:
    docker:
      - image: circleci/buildpack-deps:stretch
    steps:
      - checkout
      - setup_remote_docker
  # deploy docker to server
  deploy:
    docker:
      - image: circleci/buildpack-deps:stretch
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Prepare JSON File
          command: |
            chmod +x update_dt.sh && ./update_dt.sh
      - run: 
          name: curl command
          command: | 
            curl -X "PUT" "http://dt.klickstartonline.com:1337/api/v1/bom" -H "Content-Type: application/json" -H "X-API-Key: $DT_API_KEY" -d @bom.json

workflows:
  version: 2
  # build
  build-master:
    jobs:
      - build:
          filters:
            branches:
              only: master
  # dep check and deploy
  owasp:
    jobs:
      - deploy

